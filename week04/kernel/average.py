from typing import Tuple


class Average:
    def __init__(self, kernel_size:Tuple):
        """
        :param kernel_size: (kernel height, kernel width)
        """

        # TODO: <-- 이 부분에 코드가 한 줄 이상 들어갑니다! -->

        self.values = ??? # 생성된 kernel을 self.values에 저장